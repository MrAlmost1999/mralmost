<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mralmost.community.mapper.TagsCustomMapper">
    <select id="findTags" resultType="com.mralmost.community.model.Tags">
        select
        *
        from tags
    </select>
</mapper>